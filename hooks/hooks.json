{
  "description": "Ensure statusline is configured",
  "hooks": {
    "SessionStart": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "${CLAUDE_PLUGIN_ROOT}/scripts/statusline_setup.js"
          }
        ]
      }
    ]
  }
}
